{% trans_default_domain 'NucleosUserBundle' %}

{% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}

<div>
    <div class="alert alert-danger">
        {{ "deletion.confirmation"|trans }}
    </div>
</div>

{{ form_errors(form) }}

{{ form_start(form) }}
    {{ form_widget(form) }}
{{ form_end(form) }}
