includes:
    - phpstan-baseline.neon

parameters:
    level: max

    paths:
        - src
        - tests

    bootstrapFiles:
        - vendor-bin/tools/vendor/autoload.php

    excludes_analyse:
        - tests/bootstrap.php
